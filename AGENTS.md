# AGENTS.md

Guidance for AI coding agents working in this repo.

## Agent skills

### Issue tracker

Issues + PRDs tracked as GitHub Issues (`gh` CLI), repo `bengidev/openzone-taurians`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary; each label string equals its canonical role name. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout (`CONTEXT-MAP.md` → per-context `CONTEXT.md`). See `docs/agents/domain.md`.
